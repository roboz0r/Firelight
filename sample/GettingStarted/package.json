{
  "name": "firelight-getting-started",
  "version": "1.0.0",
  "description": "Firelight getting started examples",
  "type": "module",
  "scripts": {
    "preinstall": "dotnet tool restore",
    "clean": "rimraf build dist",
    "dev": "dotnet fable watch ./ -o ./build/ -s --run vite",
    "build": "npm run clean && dotnet fable ./ -o ./build/ --run vite build",
    "preview": "vite preview"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@lit/context": "1.1.6",
    "lit": "3.3.1"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "vite": "8.0.3"
  }
}
